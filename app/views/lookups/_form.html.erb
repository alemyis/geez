<%= simple_form_for(@lookup) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :key %>
    <%= f.input :value %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
