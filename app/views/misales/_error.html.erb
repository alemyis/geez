<% if misale.errors.any? %>
	<div id="error_explanation">
	  <h2><%= pluralize(misale.errors.count, "error") %> prohibited this misale from being saved:</h2>
	
	  <ul>
	  <% misale.errors.full_messages.each do |msg| %>
	    <li><%= msg %></li>
	  <% end %>
	  </ul>
	</div>
<% end %>